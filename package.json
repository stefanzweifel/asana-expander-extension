{
  "name": "asana-expander-extension",
  "description": "Automatically show all comments on an Asana task an expand longer comments.",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "compile": "tsc --noEmit",
    "dev": "vite",
    "build-chrome": "TARGET=chrome vite build",
    "build-ff": "TARGET=firefox vite build"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.10.1",
    "typescript": "^5.1.6",
    "vite": "^4.5.5",
    "vite-plugin-web-extension": "^3.1.2",
    "webextension-polyfill": "^0.10.0"
  }
}
